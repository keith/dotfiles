#!/bin/bash

set -euo pipefail

if [ $# -lt 1 ]; then
  echo "Usage: format-json FILE.json"
  exit 127
fi

for file in "$@"
do
  tmpdir=$(mktemp -d)
  cp "$file" "$tmpdir/file.json"
  python3 -m json.tool "$tmpdir/file.json" > "$file"
done
