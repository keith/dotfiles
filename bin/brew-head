#!/bin/sh

formula=$1

if [ -z "$formula" ]; then
  echo "Usage: brew head [FORMULA]"
  exit 1
fi

echo "Uninstalling '$formula'"
brew rm "$formula"
echo "Installing '$formula' with --HEAD"
brew install --HEAD "$formula"
