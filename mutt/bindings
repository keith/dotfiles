# Keybindings
bind attach <return> view-mailcap

bind editor <space> noop

bind index <space> collapse-thread
bind index <tab> sync-mailbox
bind index c mail
bind index G last-entry
bind index gg first-entry
bind index m change-folder
bind index p recall-message
bind index R group-reply
bind index \Cd next-page
bind index \Cu previous-page
bind index \Cw delete-thread
bind index \Cs undelete-thread

bind pager \Cd next-page
bind pager \Cu previous-page
bind pager c mail
bind pager G bottom
bind pager gg top
bind pager j next-line
bind pager k previous-line
bind pager m change-folder
bind pager R group-reply

# vim: ft=muttrc
