# Easier navigation
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias -- -="cd -"

# Shortcuts
alias g="git"
alias gs="git status"
alias gc="git clone"
alias gd="git diff"
alias gdt="git difftool"
alias gdtf="git difftool -t fugitive"
alias gba="git branch -a"
alias grv="git remote -v"
alias gsub="git submodule update --init --recursive"

alias m="mate"
alias mm="mate ."
alias s="subl"
alias ss="subl ."
alias o="open"
alias oo="open ."
alias n="nautilus"
alias v="vim"
alias vv="vim ."
alias vm="mvim"
alias vmm="mvim ."

# Vim
alias vundle="vim +BundleInstall +qall"

# Ruby/Rails
alias ruby="ruby -w"
alias bi="bundle install"
alias rg="rake generate"
alias bdgem="gem build *.gemspec; gem install *.gem --no-ri --no-rdoc; rbenv rehash"
alias rt="rails server"
alias rr="rbenv rehash"
alias macirb="macirb --simple-prompt"
alias rake="noglob rake"
alias binstubs="gem regenerate_binstubs"

# CocoaPods
alias pi="pod install"
alias psl="pod spec lint"
alias psla="pod spec lint *.podspec --verbose"
alias pclean="rm -rf $HOME/Library/Caches/CocoaPods /tmp/CocoaPods"
alias ppod="COCOA_PODS_ENV=development BUNDLE_GEMFILE=$HOME/Git/CocoaPods/Gemfile bundle exec $HOME/Git/CocoaPods/bin/pod"

# Archive Stuff
alias mktar="tar -pvczf"
alias untar="tar -zxvf"

# File size
alias fs="stat -f %z"

# SSH configuration
alias linode="ssh ksmiley@66.175.208.254"
alias rlinode="ssh -R 52698:localhost:52698 ksmiley@66.175.208.254"

# Various
alias u="uname"

# Winthrop
alias wl="ssh acc.smileyk2@tampa.winthrop.edu"
alias wa="ssh acc.smileyk2@reno.winthrop.edu"


# alias ip="curl ifconfig.me | pbcopy; echo 'Copied '`pbpaste`' to clipboard'"

# Scripts
alias xc="$HOME/Dropbox/Code/Scripts/xcworkspace.rb"
alias cloudfront="$HOME/Dropbox/Code/Scripts/cloudFront.rb"
alias mksite="$HOME/Dropbox/Code/Scripts/newSite.rb"
alias methods="$HOME/Dropbox/Code/Scripts/methods.rb"
alias pf="$HOME/Dropbox/Code/Scripts/processfile.rb"
alias lc="$HOME/Dropbox/Code/Scripts/license.rb"
alias rename="$HOME/Dropbox/Code/Scripts/imageRenamer.rb"
alias append="$HOME/Dropbox/Code/Scripts/fileAppend.rb"

# Create a new iTerm window in the same directory
function spawn { osascript $HOME/Dropbox/Code/Applescript/Spawn/SpawniTerm.applescript $PWD; }

# Detect which `ls` flavor is in use
if ls --color > /dev/null 2>&1; then # GNU `ls`
	colorflag="--color"
else # OS X `ls`
	colorflag="-G"
fi

# List all files colorized in long format
alias l="ls -l ${colorflag}"

# List all files colorized in long format, including dot files
alias la="ls -la ${colorflag}"

# Always use color output for `ls`
alias ls="command ls ${colorflag}"
export LS_COLORS='no=00:fi=00:di=01;34:ln=01;36:pi=40;33:so=01;35:do=01;35:bd=40;33;01:cd=40;33;01:or=40;31;01:ex=01;32:*.tar=01;31:*.tgz=01;31:*.arj=01;31:*.taz=01;31:*.lzh=01;31:*.zip=01;31:*.z=01;31:*.Z=01;31:*.gz=01;31:*.bz2=01;31:*.deb=01;31:*.rpm=01;31:*.jar=01;31:*.jpg=01;35:*.jpeg=01;35:*.gif=01;35:*.bmp=01;35:*.pbm=01;35:*.pgm=01;35:*.ppm=01;35:*.tga=01;35:*.xbm=01;35:*.xpm=01;35:*.tif=01;35:*.tiff=01;35:*.png=01;35:*.mov=01;35:*.mpg=01;35:*.mpeg=01;35:*.avi=01;35:*.fli=01;35:*.gl=01;35:*.dl=01;35:*.xcf=01;35:*.xwd=01;35:*.ogg=01;35:*.mp3=01;35:*.wav=01;35:'

# Enable aliases to be sudo’ed
alias sudo='sudo '

# Updates dependent on OS
update() {
  if which softwareupdate &> /dev/null;then
    echo "Running Apple software update..."
    sudo softwareupdate -ia
  fi

  if which brew &> /dev/null;then
    echo "Running homebrew updates..."
    brew update
    brew upgrade
  fi

  if which apt-get &> /dev/null;then
    echo "Running apt-get update..."
    sudo apt-get update; sudo apt-get upgrade
  fi

  if which pacman &> /dev/null;then
    echo "Running pacman update..."
    sudo pacman -Syu
  fi

  if which gem &> /dev/null;then
    echo "Running gem update..."
    $(which gem) update --system
    $(which gem) update
  fi
}

# IP addresses
alias ip="dig +short myip.opendns.com @resolver1.opendns.com | pbcopy"
alias localip="ipconfig getifaddr en1"

# Enhanced WHOIS lookups
alias whois="whois -h whois-servers.net"

# Flush Directory Service cache
alias flush="dscacheutil -flushcache"

# Clean up LaunchServices to remove duplicates in the “Open With” menu
alias lscleanup="/System/Library/Frameworks/CoreServices.framework/Frameworks/LaunchServices.framework/Support/lsregister -kill -r -domain local -domain system -domain user && killall Finder"

# Recursively delete `.DS_Store` files
# alias cleanup="find . -type f -name '*.DS_Store' -ls -delete"
alias cleanup="sqlite3 $HOME/Library/Preferences/com.apple.LaunchServices.QuarantineEventsV* 'delete from LSQuarantineEvent'"

# File size
# if stat -c '' . > /dev/null 2>&1; then
# 	# GNU `stat`
# 	alias fs="stat -c \"%s bytes\""
# else
# 	# OS X `stat`
# 	alias fs="stat -f \"%z bytes\""
# fi

# Empty the Trash on all mounted volumes and the main HDD
# Also, clear Apple’s System Logs to improve shell startup speed
alias emptytrash="sudo rm -rfv /Volumes/\*/.Trashes; sudo rm -rfv $HOME/.Trash/; sudo rm -rfv /private/var/log/asl/\*.asl"
alias secureemptytrash="sudo srm -rfv /Volumes/\*/.Trashes; sudo srm -rfv $HOME/.Trash/; sudo srm -rfv /private/var/log/asl/\*.asl"

# Show/hide hidden files in Finder
alias show="defaults write com.apple.Finder AppleShowAllFiles -bool TRUE; killall Finder"
alias hide="defaults write com.apple.Finder AppleShowAllFiles FALSE; killall Finder"

# Hide/show all desktop icons (useful when presenting)
alias hidedesktop="defaults write com.apple.finder CreateDesktop -bool false && killall Finder"
alias showdesktop="defaults write com.apple.finder CreateDesktop -bool true && killall Finder"
